import { describe, test } from '@jest/globals'

describe('validateLogin', () => {
  // TODO: returns "Email is required" when email is empty
  // TODO: returns "Invalid email format" for an invalid email
  // TODO: returns "Password must be at least 6 characters" for short passwords
  // TODO: returns null for valid email and password

  test.todo('returns "Email is required" when email is empty')
  test.todo('returns "Invalid email format" for an invalid email')
  test.todo('returns "Password must be at least 6 characters" for short passwords')
  test.todo('returns null for valid email and password')
})
